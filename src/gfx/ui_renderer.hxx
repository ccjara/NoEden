#ifndef JARALYN_UI_RENDERER_HXX
#define JARALYN_UI_RENDERER_HXX

//#include "./display.hxx"
//hud_system.hxx"

class UiRenderer {
private:
    //Display* display_ { nullptr };
    //Ui* hud_;

    //void draw_node(UiNode* node);
public:
    /*
    explicit constexpr UiRenderer(
        Display* display,
        Ui* hud
    ) : display_(display), hud_(hud) {
        assert(display_);
        assert(hud_);
    }
    */

    void draw();
};

#endif
